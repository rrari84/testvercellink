{
  "buildCommand": "cd frontend && npm run build:client",
  "outputDirectory": "frontend/dist/spa",
  "installCommand": "cd frontend && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}